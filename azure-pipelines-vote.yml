# Docker
# Build and push an image to Azure Container Registry
# https://docs.microsoft.com/azure/devops/pipelines/languages/docker

trigger:
  paths:
    include:
      - vote/*

resources:
- repo: self

variables:
  # Container registry service connection established during pipeline creation
  dockerRegistryServiceConnection: '7fba8884-305c-4403-9110-7d92c09485dc'
  imageRepository: 'votingapp'
  containerRegistry: 'rahulazurecicd.azurecr.io'
  dockerfilePath: '$(Build.SourcesDirectory)/vote/Dockerfile'
  tag: '$(Build.BuildId)'

  # Agent VM image name
  vmImageName: 'ubuntu-latest'

stages:
- stage: Build 
  displayName: Build 
  jobs:
  - job: Build
    displayName: Build 
    pool:
      vmImage: $(vmImageName)
    steps:
    - task: Docker@2
      displayName: Build and push an image to container registry
      inputs:
        containerRegistry: '$(dockerRegistryServiceConnection)'
        repository: '$(imageRepository)'
        command: 'buildAndPush'
        Dockerfile: 'vote/Dockerfile'
        tags: '$(tag)'
- stage: Update 
  displayName: Update 
  jobs:
  - job: Update
    displayName: Update 
    pool:
      vmImage: $(vmImageName)
    steps:
      - script: sed -i 's/\r$//' scripts/updateK8sManifests.sh
        displayName: Convert script to LF
      - task: ShellScript@2
        displayName: Update
        inputs:
          scriptPath: 'scripts/updateK8sManifests.sh'
          args: 'vote $(containerRegistry) $(imageRepository) $(tag)'
        env:
          SYSTEM_ACCESSTOKEN: $(System.AccessToken)
